from setuptools import setup, find_packages

setup(
    name="matrix-invite-safety",
    version="1.0.1",
    packages=find_packages(),
    description="Safe invites on Matrix",
    include_package_data=True,
    zip_safe=True,
    install_requires=[],
)
